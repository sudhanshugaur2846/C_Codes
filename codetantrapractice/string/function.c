#include <stdio.h>
#include<string.h>
int main()
{
    char s1[50];
    char s2[11];
    //printf("%s",strcat("hello","world"));
    //strlen("hello");
    //char a[]="gaur";
    //char b[]="gaur";
    //puts(strcat(a,b));
    //printf("%s",strcat(a,b));
    //puts(strlen(a));
    //printf("%d",strlen(a));
    //puts(strrev(a));
    //printf("the reverse is %s",strrev(a));
    //puts(a);
    //puts(strcmp(a,b));
    //printf("the starcmpp si %d",strcmp(a,b));
    //puts(a);
    gets(s1);
    gets(s2);
    puts(strcat(s1,s2));
    return 0;
}
