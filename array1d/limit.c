#include <stdio.h>
#include <limits.h>
int main()
{
    int min,max;
    min=INT_MIN;
    max=INT_MAX;
    printf("The minimum value is %d\n",min);
    printf("The minimum value is %d\n",max);
    
    return 0;
}
